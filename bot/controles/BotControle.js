export class BotControle {
    static atualizarParticipantesGrupo(socket, update) {
        console.log('Atualizando participantes do grupo:', update);
        
    }

    static atualizarDadosGrupo(socket, update) {
        console.log('Atualizando dados do grupo:', update);
        
    }
}
